#
# Example configuration file. You probably want to copy it to ~/.arc.conf
#

[server]
host = autotest-server.example.com

[job]
test_type = Client
priority = Low
reboot_before = If dirty
reboot_after = Never
machines = autotest-client.example.com
