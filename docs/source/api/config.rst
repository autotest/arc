.. _arc-api-config:

:mod:`config` --- Autotest RPC Client Configuration
===================================================

The :mod:`config` module provides the means for users of both the Arc API and the arcli command line application to have defaults that suit their need.

.. automodule:: arc.config
   :members:
